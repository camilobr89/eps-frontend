services:
  eps-frontend:
    build:
      context: .
    image: eps-frontend
    ports:
      - "${FRONTEND_PORT:-4173}:4173"
    environment:
      - API_PROXY_TARGET=${API_PROXY_TARGET:-http://host.docker.internal:3000}
